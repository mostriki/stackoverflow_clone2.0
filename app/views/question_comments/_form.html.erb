<%= render "layouts/errors", :object => @question_comment %>

<%= form_for [@question, @question_comment] do |f| %>
  <%= f.label :body %><br>
  <%= f.text_area :body %><br>
  <%= f.hidden_field(:user_id, :value => current_user.id) %>
  <%= f.submit %>
<% end %>
